{
  "hooks": [
    {
      "name": "validate-secret-output",
      "description": "Blocks plaintext-shaped output only when the tool result is explicitly marked as SVLT-managed. Never request plaintext from MCP. User-selected plaintext and other providers remain outside this opt-in hook.",
      "events": [
        "tool_result"
      ],
      "command": "node",
      "args": [
        "${PLUGIN_ROOT}/hooks/validate_secret_output.js"
      ]
    }
  ]
}
